# password-generator
A Python program that checks whether a password is strong based on security rules like length, uppercase, lowercase, digits, and symbols. If the password is weak, it automatically generates a stronger, more secure version.
